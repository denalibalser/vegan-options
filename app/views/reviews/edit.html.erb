<h3>Editing Your Review, <%= current_user.username %>:</h3>

<%= form_for [ @review ] do |f| %> <%# deleted @restaurant, from [@review] %>

    <p>
      <%= f.text_area :body %>
    </p>
    <p>
      <%= f.submit 'Submit' %>
    </p>
<% end %>

<%= link_to "Delete Review", review_path(@review), data: {:confirm => 'Are you sure?'}, :method => :delete %>

<%# restaurant_review_path(@restaurant, @review) %>